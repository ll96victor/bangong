# 工单助手与Task客服信息提取合并版 维护与交接文档

## 1. 文档目的

本文档用于后续维护、功能扩展、问题排查与多人/多 AI 交接。

目标是让接手者在不反复通读 20 多万字符脚本的前提下，快速掌握：

- 这个脚本做什么
- 它运行在哪些页面
- 各模块之间怎么分工
- 哪些状态和存储是关键
- 最近改过什么，为什么这样改
- 当前仍有哪些风险和注意事项
- 开发过程中总结出的经验

当前主维护文件：

- `工单助手与Task客服信息提取合并版 6.8.0-6.8.0.user.js`（文件名保留历史命名；**以脚本头 `@version` / `@name` 为准**，当前与 **6.8.7** 一致）

当前脚本头信息状态（截至文档同步时）：

- `@version`：**6.8.7**
- `@name`：**工单助手与Task客服信息提取合并版 6.8.7**

若后续再次出现文件名与脚本头版本不一致，以 **油猴中安装的脚本头** 为发版真源。

---

## 2. 当前目录与文件角色

当前目录中同时存在多个历史版本脚本和辅助文档。

重点文件：

- `工单助手与Task客服信息提取合并版 6.8.0-6.8.0.user.js`
  当前实际维护主文件
- `工单助手与Task客服信息提取合并版 6.8.0-6.8.0.user - 副本.js`
  当前主文件的副本，不应默认视为同步最新
- `工单助手与Task客服信息提取合并版-优化建议.md`
  之前整理过的一轮优化建议文档
- `rules - 副本.md`
  本项目这轮维护时反复参考的规范文档

建议原则：

- 后续修改默认只改当前主文件
- 发布稳定版本后，再视需要另存一个带版本号的快照
- 不要在主文件和副本文件同时改，容易产生“哪个才是最新”的混乱

---

## 3. 脚本定位与运行范围

这是一个多模块合并油猴脚本，核心作用是提升 AIHelp 工单处理效率，并补充飞书搜索和 GM 工具自动化。

### 3.1 主要能力

- 普通工单：复制内部描述、自动识别区服、自动翻译并处理标题
- MCGG 工单：复制描述、自动处理 MCGG 标题
- Task 信息：提取客服信息并复制链接
- 自动回复：提交后自动点击“内部回复”并填充固定文本
- 飞书联动：从工单中提取 `Ticket ID`，到飞书项目页自动搜索
- 清除头像：跨页打开 GM 工具，自动执行清除头像冷却时间
- 内部回复快捷入口：从状态栏图标或展开面板直接点击

### 3.2 运行页面

脚本当前通过 `@match` 覆盖：

- `https://ml-panel.aihelp.net/*`
- `https://ml.aihelp.net/*`
- `https://aihelp.net.cn/*`
- `https://aihelp.net/*`
- `https://project.feishu.cn/ml/workObjectView/onlineissue/Cot68m5vg`
- `https://project.feishu.cn/ml/workObjectView/onlineissue/Cot68m5vg?*`
- `https://gm.moba.youngjoygame.com:8090/*`

同时通过 `@exclude` 排除：

- `*://*/dashboard/#/newpage-ticket*`

脚本启动后还有一层运行时判断：

- URL 包含 `ticket` 时直接退出
- AIHelp 主逻辑只在 `task?orderId` 或 `tasks?searchType` 页面运行
- 飞书执行器只在固定飞书目标页运行
- GM 模块只在 GM 工具域名运行

---

## 4. 模块总览

脚本采用“多个独立 IIFE 模块 + 少量共享工具”的组织方式。

### 4.1 公共层

公共层主要包含：

- 页面判断函数
- 状态栏 `StatusbarUI`
- 共享工具 `SharedUtils`
- MCGG 判断配置 `MCGG_CONFIG`

### 4.2 模块列表

#### 模块 A：普通工单助手

主要职责：

- 提取“内部描述”
- 根据 `ServerID` 判定全服/测服
- 自动处理“任务标题”
- 自动处理“关联第三方”中的 `发现迭代`、`渠道`
- 提供普通工单复制功能

当前重要行为：

- 标题无冒号也会进入翻译流程
- 中文标题会保留交互面板
- 先处理标题，再处理 `发现迭代`，最后处理 `渠道`
- 关联第三方使用 `.tabDetail-item.thirdLink` 内行标题定位；展开 `el-select` 时优先匹配 **`.custom-down-select-search`** 弹出层，依次尝试 **input 点击 / 箭头 / 合成事件**；`fillDropdownSearch` 在自动链路中带 **`preferThirdLink`**（与手动焦点兜底共用 `SharedUtils`）
- 「渠道」若界面当前值已与 ServerID 推断的全服/测服一致则跳过自动选择
- 自动处理前约 **400ms** 延迟，便于「关联第三方」区域渲染完成
- 如果自动填充失败，仍保留焦点监听作为兜底

#### 模块 B：MCGG 工单助手

主要职责：

- 提取“描述”字段
- 根据 `ServerID` 判定标题前缀
- 自动处理 MCGG 标题
- 在标题处理完成后，同样自动处理「关联第三方」中的 `发现迭代`、`渠道`（逻辑与模块 A 对齐）
- 提供 MCGG 描述复制功能

#### 模块 C：Task 客服信息提取

主要职责：

- 提取工单链接
- 提取客服前缀与名称
- 复制为 `链接 + @客服名`

#### 模块 D：提交后自动回复

主要职责：

- 监听工单页面“确认/确定”点击
- 排除批量处理和批量筛选场景
- 自动打开“内部回复”
- 自动填充固定文本 `already submitted`
- 自动点击回复按钮

#### 模块 E-AIHelp 端：飞书搜索触发器

主要职责：

- 监听工单切换
- 提取 `Ticket ID`
- 将搜索任务写入 GM 存储（含 `ticketId`、`requestTs`、**`searchRequestId`**，便于与飞书端控制台日志对照）
- 唤起或复用飞书目标页
- 轮询飞书端结果并展示在日志面板

#### 模块 E-飞书端：飞书搜索执行器

主要职责：

- 仅在指定飞书目标页运行
- 轮询待搜索任务
- 点击“查找”
- 输入 `Ticket ID`
- 点击“过滤”
- 判断结果并回写 GM 存储

#### 模块 F-AIHelp 端：清除头像

主要职责：

- 从工单中提取 `UID` 和 `ServerID`
- 写入跨域任务
- 打开 GM 工具页面
- 轮询读取 GM 端日志

#### 模块 F-GM 端：清除头像执行器

主要职责：

- 读取 AIHelp 写入的任务
- 在 GM 工具页自动选择区服、输入 UID、执行操作
- 实时写日志到 GM 存储

#### 模块 G：内部回复快捷入口

主要职责：

- 在状态栏和展开面板中提供“内部回复”快捷按钮
- **未打开**内部回复对话框时：自动点击工单工具栏上的「内部回复」或「内部备注」
- **已打开**标题为「内部回复」的对话框（如 `.cusHeader` 文案为内部回复）时：自动点击对话框底部 **「回复」**（与再次打开工具栏按钮区分）

---

## 5. UI 结构说明

### 5.1 小图标六宫格

状态栏图标是 2 列 x 3 行，共 6 格：

- 第 1 格：普通工单 `N`
- 第 2 格：MCGG `M`
- 第 3 格：Task `T`
- 第 4 格：展开 `⚡`
- 第 5 格：清除头像 `清`
- 第 6 格：内部回复 `内`

### 5.2 展开面板按钮

当前展开面板有 5 个按钮：

- `复制普通工单`
- `复制MCGG工单`
- `提取客服信息`
- `清除头像`
- `内部回复`

这 5 个按钮刚在本轮维护中重新确认过，后续改文案时不要误改按钮数量和顺序。

---

## 6. 关键共享工具说明

`SharedUtils` 是当前脚本最重要的公共能力层，很多功能都依赖它。

### 6.1 当前关键方法

- `getCurrentTicketID()`
  当前已优化为：
  - 先从 URL 提取 `orderId`
  - 再扫描局部容器
  - 最后回退到 `document.body`

- `findTitleInputRobust()`
  标题输入框的鲁棒查找逻辑

- `extractTextByFormLabels()`
  基于字段名的 outerHTML 提取逻辑

- `extractContentWithImages()`
  文本提取时保留图片 URL

- `copyText()`
  复制时先走 `navigator.clipboard`
  失败后回退 `GM_setClipboard`

- `waitForDropdownSearchInput()`
  查找当前展开下拉框中的搜索框（可选 `preferThirdLink`，优先 `.custom-down-select-search`）

- `fillDropdownSearch()`
  向下拉搜索框输入文本，并尝试自动点选匹配项（可选 `preferThirdLink`）

- `selectDropdownOption()`
  不再只是“输入关键词”，而是尽量真正选中下拉项

- `findThirdLinkFieldRow` / `findThirdLinkFieldInput` / `getThirdLinkFieldDisplayValue`
  关联第三方区域内按标签定位行与输入框

- `openThirdLinkElSelectDropdown(labelTexts, fieldName, logFn)`
  打开关联第三方 `el-select` 下拉（input / caret / 合成事件多重尝试）

- `channelDisplayMatchesDesired(displayValue, desiredChannel)`
  判断渠道当前展示是否与目标全服/测服一致，用于跳过重复操作

---

## 7. 关键状态与存储键

### 7.1 `localStorage`

来自 `StatusbarUI.STORAGE_KEYS`：

- `feishu_tools_status_bar_position_v1`
- `feishu_tools_log_panel_position_v1`
- `feishu_tools_log_panel_size_v1`

作用：

- 记录状态栏位置
- 记录日志面板位置
- 记录日志面板大小

### 7.2 翻译相关 GM 存储

- `glm_api_key_v1`
- `glm_model_version_v1`

作用：

- 智谱翻译 API Key
- 智谱翻译模型选择

### 7.3 飞书搜索 GM 存储

AIHelp 端与飞书端共用：

- `feishu_ticket_search_pending_v1`
  待搜索任务（对象字段：`ticketId`、`requestTs`、**`searchRequestId`**，后者用于日志与排障对照）
- `feishu_ticket_search_result_v1`
  搜索结果
- `feishu_ticket_search_result_ts_v1`
  搜索结果时间戳
- `feishu_ticket_search_heartbeat_v1`
  飞书目标页心跳
- `feishu_ticket_search_open_lock_v1`
  本轮新增，飞书目标页打开锁

### 7.4 清除头像 GM 存储

通过前缀 `clear_avatar_` 组织：

- `clear_avatar_pending_task`
- `clear_avatar_popup_mode`
- `clear_avatar_log_data`

作用：

- 待执行任务
- 弹窗模式标记
- GM 工具执行日志

---

## 8. 近期重要维护记录

### 8.1 第一批低风险整理

已完成内容：

- 统一 AIHelp 目标页面判断
- 删除未使用翻译源与失效选择器
- 调试日志默认恢复为开启
- 状态栏视觉重绘
- 飞书页面匹配收窄到目标页

### 8.2 第二批中风险优化

已完成内容：

- `getCurrentTicketID()` 收敛优化
- 普通工单与 MCGG 复制链路补强
- 复制支持 `navigator.clipboard` -> `GM_setClipboard` 回退
- 普通工单支持 outerHTML 优先提取
- MCGG 支持 outerHTML 优先提取
- 模块 A 自动顺序处理：
  - 任务标题
  - 发现迭代
  - 渠道
- 飞书目标页新增“打开锁”，减少重复开页和任务被误消费

### 8.3 第三批（v6.8.5–v6.8.7）：关联第三方、模块 G、飞书日志

已完成内容：

- 修复油猴环境下 `MouseEvent` 的 `view` 导致关联第三方下拉无法展开；`openThirdLinkElSelectDropdown` 与 `preferThirdLink` 下拉识别
- 关联第三方：发现迭代 / 渠道自动填充与 MCGG 对齐；渠道与当前值一致时跳过
- 模块 G：内部回复对话框已打开时改点底部「回复」
- 飞书：每次搜索生成 `searchRequestId`，写入 `feishu_ticket_search_pending_v1`，AIHelp 与飞书端日志带同一前缀便于对照
- 脚本头 `@name` / `@version` 与 **6.8.7** 对齐（文件名仍为 `6.8.0-6.8.0` 历史命名）

---

## 9. 当前已知问题与风险

### 9.1 元信息版本不一致

**状态：已处理（v6.8.5 起）**：脚本头 `@name` 与 `@version` 已统一为同一版本号（当前 **6.8.7**）。

**仍须注意**：仓库内文件名仍为 `工单助手与Task客服信息提取合并版 6.8.0-6.8.0.user.js`，与版本号数字不同，交接时以脚本头为准。

### 9.2 飞书搜索仍属于高波动 DOM 自动化

虽然本轮增加了：

- 心跳复用
- 目标页打开锁
- 更长超时
- 搜索中不消费新任务

但飞书页面本质仍是重型 SPA，DOM 结构或交互机制一变，搜索流程仍可能失效。

重点风险点：

- “查找”按钮结构变化
- 搜索框 placeholder 变化
- “过滤”复选框结构变化
- 结果判断依赖 `X/X` 计数和空态文本，不是官方 API

### 9.3 下拉框自动选择对页面实现有依赖

模块 A 和模块 B 的下拉自动处理目前依赖：

- `.el-select-dropdown`
- `.el-select-dropdown__item`
- 展开后存在搜索输入框

如果 AIHelp 后台升级了 Element UI 版本或组件实现，自动填充可能先失效。

### 9.4 `currentUrl` 存在多处局部重名

脚本中多个 IIFE 内都定义了自己的 `currentUrl`。

当前能正常运行，但维护时要注意：

- 不同模块引用的是自己的局部变量，不一定是同一个
- 拆代码或移动代码块时容易引入作用域错误

### 9.5 多模块仍然是多套轮询

目前多个模块仍各自维护 `setInterval`。

这在可读性和维护性上仍然偏重，后续如果继续中高风险重构，可以考虑统一“工单切换总线”。

---

## 10. 排障与验证方法

### 10.1 最基础检查

每次改完脚本，先跑：

```powershell
node --check "工单助手与Task客服信息提取合并版 6.8.0-6.8.0.user.js"
```

这一步只能保证语法正确，不能保证页面行为正确。

### 10.2 建议的人工验证顺序

#### 普通工单

验证项：

- 是否能提取“内部描述”
- 点击 `复制普通工单` 是否稳定复制
- 标题是否自动更新
- `发现迭代` 是否自动填充
- `渠道` 是否自动填充

#### MCGG 工单

验证项：

- 是否能提取“描述”
- 点击 `复制MCGG工单` 是否稳定复制
- 标题前缀是否正确
- 「关联第三方」中 `发现迭代`、`渠道` 是否自动填充（与模块 A 行为一致）

#### Task 信息

验证项：

- 点击 `提取客服信息` 是否复制 `链接 + @客服`

#### 自动回复

验证项：

- 单工单提交后是否自动打开内部回复
- 是否能填入 `already submitted`
- 批量处理场景是否正确跳过

#### 飞书搜索

验证项：

- 同一工单触发时是否仍重复开多个飞书目标页
- 单一目标页是否能在合理时间内回写结果
- 结果返回后目标页是否会自动刷新

#### 清除头像

验证项：

- 是否能从工单正确提取 UID / ServerID
- 是否能打开 GM 工具并自动执行
- 日志面板是否能同步 GM 端日志

#### 内部回复快捷（模块 G，v6.8.6+）

验证项：

- 未弹出内部回复对话框时，点击状态栏「内」或展开面板「内部回复」是否打开对话框
- 对话框已打开时，再次点击上述按钮是否触发底部「回复」（非再次点工具栏「内部回复」）

### 10.3 v6.8.7 回归清单（浏览器内人工）

说明：须在 **ml-panel** 等真实页面逐项验证；仓库内仅能执行 `node --check` 做语法检查，**不能替代**下列勾选。

- [ ] 普通工单：内部描述、标题、发现迭代、渠道（自动填充 + 焦点兜底）
- [ ] MCGG：描述、标题、发现迭代、渠道
- [ ] 模块 G：无对话框打开内部回复；有对话框点击「回复」
- [ ] 自动回复：单工单确认后 `already submitted`；批量/筛选场景跳过
- [ ] 飞书：触发搜索、结果回写；日志中 `[fs_时间戳_随机]` 与飞书标签页控制台可对照
- [ ] 清除头像（若使用）：任务下发与日志

### 10.4 推荐日志观察点

日志面板会按模块标签显示：

- `normal`
- `mcgg`
- `task`
- `feishu`
- `clear`
- `reply`
- `autoReply`

排障时优先看日志面板，再看控制台。

---

## 11. 后续维护建议

### 11.1 低风险可继续做的事

- 将各版本更新记录持续写入脚本头部注释（当前已含至 v6.8.7）
- 整理一份“常见页面 outerHTML 样例库”（可选，便于多人对齐 DOM）

### 11.2 中风险可继续做的事

- 统一多个模块的工单切换监听
- 收敛重复的 `setInterval`
- 将 AIHelp 下拉框自动处理抽成更稳定的字段驱动方案
- 将飞书结果检测从“页面文本猜测”进一步改为更明确的 DOM 区域判断

### 11.3 高风险暂不建议立刻做的事

- 一次性把所有模块重写成类或总线架构
- 大规模重命名全局变量与模块状态对象
- 直接去掉当前的兜底逻辑

原因：

- 当前脚本是“生产中不断补洞”的演进型脚本
- DOM 自动化场景下，完全整洁不如“能跑且有兜底”重要
- 大重构容易引入链式回归

---

## 12. 开发过程经验总结

这部分是本轮维护最值得保留给后续接手者的内容。

### 12.1 DOM 自动化不要只相信“结构稳定”

实际情况是：

- 相同页面在不同加载阶段，DOM 差异很大
- 同一字段可能有多种渲染方式
- 只靠一个选择器很容易偶发失效

有效做法是多层兜底：

- outerHTML 优先
- 精确 DOM 提取次之
- 文本扫描最后兜底

### 12.2 复制功能不能只依赖一个剪贴板 API

在油猴环境里，`navigator.clipboard` 有时受页面权限或浏览器策略影响。

经验结论：

- 先用 `navigator.clipboard`
- 失败后立即回退 `GM_setClipboard`

这比单一路径稳定得多。

### 12.3 下拉框自动化不能停在“输入关键字”

本轮一个明确教训是：

- 把字填进搜索框，不代表值真的选中了

所以后来补了：

- 展开下拉框
- 输入关键字
- 再尝试真正点击匹配项

如果后续还有类似字段，优先复用这一思路。

### 12.4 飞书这种重型 SPA，单靠心跳还不够

之前只做“心跳复用”还不够稳，原因是：

- 目标页刚打开但脚本还没开始写心跳时
- AIHelp 端可能又触发了一次打开

后来补上的关键是“打开锁”：

- 页面还没活过来之前，先标记“已经在打开中”
- 减少重复开多个目标页

这类跨页自动化，通常需要：

- 心跳
- 打开锁
- 请求时间戳
- 结果时间戳

四者一起用才更稳。

### 12.5 生产脚本重构要分批做

这份脚本已经是多模块长期迭代结果。

经验上不要一次动太多层：

- 第一批先做低风险清理
- 第二批再做中风险优化
- 每批都要能单独回归

这比“想一次性整理漂亮”更实际。

### 12.6 注释和文档非常重要

这类脚本的问题不是“功能少”，而是：

- 页面多
- 状态多
- 兜底多
- 时间久了很容易忘记为什么这么写

所以后续维护时，建议遵守：

- 新增一个跨页状态，就写明用途
- 新增一个兜底分支，就写明触发原因
- 改动一轮中风险逻辑，就同步更新维护文档

---

## 13. 后续接手 checklist

后续无论是人还是 AI 接手，建议按下面顺序进行：

1. 先读本文件，再读主脚本头部更新记录。
2. 确认当前真正维护的是哪个主文件，不要误改副本。
3. 修改前先跑一次全文搜索，确认相关模块的调用链。
4. 改动后先跑 `node --check`。
5. 至少人工验证：
   - 普通工单
   - MCGG 工单
   - 飞书搜索
   - 自动回复
6. 如果改动涉及 GM 存储键，必须同步检查 AIHelp 端和飞书/GM 端是否一致。
7. 如果改动涉及字段提取，优先保留旧逻辑兜底，不要直接删回退路径。

---

## 14. 建议的下次发版整理项

发新版本时建议顺手：

- 保持 `@name` 与 `@version` 一致（当前已对齐 **6.8.7**）
- 在脚本头部注释增加该版本变更摘要（可参考本文件 §8）
- 将本维护文档中的「已知问题」「近期重要维护」与脚本头简版注释同步更新

---

## 15. 交接结论

当前脚本并不是“全新重构完成态”，而是“已经可维护、可继续演进”的阶段。

它的维护策略应当是：

- 保持模块边界清晰
- 在高波动 DOM 场景下保留多层兜底
- 用小步快跑替代一次性大重构
- 每一轮改动后补文档，减少未来维护成本

如果后续继续迭代，这份文档应当随版本一起更新，而不是只写一次。
