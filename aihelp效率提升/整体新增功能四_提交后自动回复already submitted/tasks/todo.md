# 2026-03-23

- [x] 模块 A：将多源面板展示时序改为渠道稳定后 100ms 展示，渠道阶段超过 2 秒强制展示
- [x] 模块 A：放宽普通工单“发现迭代”成功判定，兼容版本号命中扩展选项文案
- [x] 模块 A：收敛“发现迭代”重复失败日志，失败时仅保留单一结论
- [x] 模块 A：腾讯翻译仅保留 SecretId / SecretKey 菜单配置，Region/ProjectId 改为脚本默认值
- [x] 同步更新脚本版本与头部变更记录

## Comment

- 本次只修改模块 A，不扩散到 MCGG。
- 额外修正了腾讯翻译菜单注册位置，避免其落在 `initUI()` 外部。

# 2026-03-24

- [x] 模块 A：发现迭代关闭“唯一候选”兜底，避免候选未刷新时误点旧版本
- [x] 模块 A：工单切换后为标题处理、发现迭代、渠道自动填充增加旧链路失效保护
- [x] 模块 B（MCGG）：同步补上发现迭代唯一候选收紧和工单切换失效保护
- [x] 同步更新脚本版本、维护与交接文档、回归清单

## Comment

- 本次是最小改动，没有重构公共下拉逻辑，只对“发现迭代”收紧唯一候选兜底。
- 已执行 `node --check`，通过语法检查；页面行为仍需在真实 AIHelp 页面人工回归。

# 2026-03-24（第二轮）

- [x] SharedUtils：增加可见关联第三方下拉弹层清理，打开新字段前先清空旧搜索词并关闭旧弹层
- [x] 模块 A / MCGG：工单切换重置时同步清理旧关联第三方下拉，减少 `模式独立包` / `2.1.66` / `2.1.60` 串到新工单
- [x] 同步更新脚本版本、维护与交接文档、回归清单

## Comment

- 这轮判断问题更像“旧下拉弹层残留”，不是单纯 `ServerID` 或版本目标值没覆盖。
- 已再次执行 `node --check`，通过语法检查；仍需真实页面验证切单场景。
