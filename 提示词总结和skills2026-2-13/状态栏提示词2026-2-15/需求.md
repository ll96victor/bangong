我正在通过 AI 生成多个油猴脚本，脚本中都包含一个状态栏 UI。请帮我做两件事：

---

## 第一件事：内存占用分析

请分析状态栏的内存占用情况，具体包括：

1. **基础状态栏**：一个简单的状态栏（DOM 节点约 10-50 个）本身内存占用大吗？
2. **可拖拽/可隐藏**：增加拖拽和隐藏功能后，内存开销会明显增加吗？
3. **调试日志面板**：如果状态栏作为调试日志面板持续输出日志，会导致内存溢出吗？如何通过代码规避？（请在分析中给出具体方案）

---

## 第二件事：生成 .md 格式的 Skills（技能文档）

请帮我生成一个可以直接复制的 `.md` 格式的 System Prompt（系统提示词），用于指导 AI 生成规范的状态栏代码。

### 功能需求：

**1. 三种核心功能：**
- 点击提取并复制页面信息（可配置 CSS 选择器）
- 点击打开指定链接
- 作为调试日志面板，实时显示脚本运行日志

**2. 双形态切换（必需功能）：**
- **紧凑模式**：默认显示为 32x32 像素的纯图标小圆点
- **展开模式**：交互后展开显示图标 + 文字描述 + 功能按钮 + 日志区域
- **关键约束**：双形态切换**不能**影响核心功能（复制、打开链接），点击事件必须正确绑定

**3. 交互增强：**
- 支持拖拽移动位置
- 支持一键隐藏（通过关闭按钮 ×）

**4. 日志面板专项需求：**
- **内存安全**：必须限制日志最大行数（如 100 条），超出后自动删除最旧的日志节点
- **关闭按钮**：日志面板右上角必须有"×"关闭按钮，点击后折叠为紧凑模式
- **配置开关**：通过配置项 `showLogPanelByDefault: true/false` 控制是否默认展开日志面板

### 文档格式要求：

- 输出为 `.md` 格式的 System Prompt
- **重要**：此 skill.md 会与需求.md 文档一起上传给 AI，因此 **skill.md 中不需要包含以下元信息**（由需求.md 提供）：
- skill.md 应聚焦于：代码实现模板、内存安全规范、功能逻辑、配置项设计

### 输出要求：

请结合上述分析，给出一份完整的 Skills 文档，包含：
- 内存安全规范（特别是日志清理逻辑）
- 完整的代码实现模板（类封装、事件绑定、配置项）
- 功能逻辑说明（双形态切换、复制/链接功能实现）


  